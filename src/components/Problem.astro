<section class="section consequences">
  <div class="container">
    <div class="consequences-inner fade-in">
      <h2 class="consequences-headline">
        Cosa succede se non fai nulla
      </h2>

      <div class="consequences-body">
        <p class="consequences-lead fade-in fade-in-delay-1">
          Niente di drammatico. Il mercato non crolla domattina.
        </p>

        <p class="fade-in fade-in-delay-1">
          Le decisioni strategiche continuano a passare tutte da te — e il tempo per pensare si riduce ogni mese. I concorrenti che integrano direzione e AI guadagnano terreno, non con una mossa — con cento piccole mosse che tu non vedi perché sei dentro l'operatività.
        </p>

        <p class="fade-in fade-in-delay-2">
          Il team esegue. Nessuno guida. Le tattiche si accumulano senza strategia.
        </p>

        <p class="consequences-closing fade-in fade-in-delay-2">
          "Ha sempre funzionato così" è il bias più costoso nelle PMI italiane. Non perché sia sbagliato — perché il contesto in cui funzionava non esiste più.
        </p>
      </div>
    </div>
  </div>
</section>

<style>
  .consequences {
    background: var(--color-navy);
    color: var(--color-bianco);
  }

  .consequences-inner {
    max-width: 720px;
    margin: 0 auto;
  }

  .consequences-headline {
    font-size: clamp(1.75rem, 4vw, 2.75rem);
    color: var(--color-bianco);
    margin-bottom: 2.5rem;
    text-align: center;
  }

  .consequences-body {
    display: flex;
    flex-direction: column;
    gap: 1.5rem;
  }

  .consequences-body p {
    font-size: 1.0625rem;
    line-height: 1.8;
    color: rgba(255, 255, 255, 0.8);
    margin: 0;
  }

  .consequences-lead {
    font-size: 1.1875rem !important;
    color: rgba(255, 255, 255, 0.6) !important;
    font-style: italic;
  }

  .consequences-closing {
    color: var(--color-rosso-light) !important;
    font-weight: 500;
    border-left: 3px solid var(--color-rosso-light);
    padding-left: 1.5rem;
  }
</style>
